import styled from "styled-components";

const Horizontal = styled.div`
  display: flex;
  padding: 24px;
  border-radius: 10px;
  background-color: #45455c;
  justify-content: space-between;
`;

const Column = styled.div`
  padding: 12px;
`;

export default { Horizontal, Column };
